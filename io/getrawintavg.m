function rawIntensityAvg = getrawintavg(rawimage, points)
% gets the average intensity from points and raw image

    numPoints = size(points, 1);
    rawIntensityAvg
    for p = 1:numPoints
        rawIntensityAvg
    end

end